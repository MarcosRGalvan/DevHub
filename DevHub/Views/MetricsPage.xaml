<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DevHub.Views.MetricsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevHub.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Métricas de Rendimiento del Sistema" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="24" VerticalAlignment="Top">

            <Border Width="220" Height="220" Padding="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                    <TextBlock Text="Carga de la CPU" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center" />

                    <ProgressRing Value="{x:Bind ViewModel.CpuUsage, Mode=OneWay}"
                                  IsIndeterminate="False" Width="90" Height="90" />

                    <TextBlock Text="{x:Bind ViewModel.CpuUsage, Mode=OneWay}"
                               FontSize="14" HorizontalAlignment="Center" FontWeight="Medium" />
                    
                </StackPanel>
            </Border>

            <Border Width="220" Height="220" Padding="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                    <TextBlock Text="Memoria del Sistema" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center" />

                    <ProgressRing Value="{x:Bind ViewModel.RamPercentage, Mode=OneWay}"
                                  IsIndeterminate="False" Width="90" Height="90"
                                  Foreground="{ThemeResource SystemControlHighlightAccentBrush}"/>

                    <TextBlock Text="{x:Bind ViewModel.RamUsageStr, Mode=OneWay}"
                               FontSize="12" Foreground="{ThemeResource AccentTextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center" TextAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Width="200" Height="220" Padding="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                    <TextBlock Text="GPU Interna (Intel Iris Graphycs)" TextWrapping="Wrap" TextAlignment="Center" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <ProgressRing Value="{x:Bind ViewModel.GpuIntegratedUsage, Mode=OneWay}" IsIndeterminate="False" Width="85" Height="85" Foreground="#FF107C41" />
                    <TextBlock Text="{x:Bind ViewModel.GpuIntegratedUsage, Mode=OneWay}" FontSize="14" HorizontalAlignment="Center" FontWeight="Medium"/>
                </StackPanel>
            </Border>

            <Border Width="200" Height="220" Padding="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                    <TextBlock Text="GPU Dedicada (NVIDIA RTX A1000)" TextWrapping="Wrap" TextAlignment="Center" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <ProgressRing Value="{x:Bind ViewModel.GpuDedicatedUsage, Mode=OneWay}" IsIndeterminate="False" Width="85" Height="85" Foreground="#FFE81123"/>
                    <TextBlock Text="{x:Bind ViewModel.GpuDedicatedUsage, Mode=OneWay}" FontSize="14" HorizontalAlignment="Center" FontWeight="Medium"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
